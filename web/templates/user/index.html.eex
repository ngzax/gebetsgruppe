<h2>Users</h2>

<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td><b><%= user.id %></b></td>
      <td><%= user.name %></td>
      <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table>
